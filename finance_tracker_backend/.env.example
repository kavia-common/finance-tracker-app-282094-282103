# Database connection string (SQLite by default)
DATABASE_URL=sqlite:///./data.db

# JWT configuration (placeholder secret; change in production)
JWT_SECRET=change-me
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
